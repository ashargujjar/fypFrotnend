# FYP Frontend (Vite + React + TypeScript + Tailwind)

Short scaffold for a frontend using Vite, React, TypeScript and Tailwind CSS.

Quick start

1. Install dependencies:

```cmd
cd d:\\Projects\\fyp
npm install
```

2. Start development server:

```cmd
npm run dev
```

3. Build:

```cmd
npm run build
```
